var input = require('./InputEntry'),
    radio = require('./RadioEntry'),
    checkbox = require('./CheckboxEntry'),
    select = require('./SelectEntry'),
    textarea = require('./TextareaEntry');

module.exports = {
    input: input,
    radio: radio,
    checkbox: checkbox,
    select: select,
    textarea: textarea
};